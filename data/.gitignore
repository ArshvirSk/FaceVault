# Keep the empty directories that the app creates at runtime,
# but ignore everything else (database, photos, cache files).
*
!.gitignore
!.gitkeep
!cache/
!cache/.gitkeep
!profile_photos/
!profile_photos/.gitkeep
!uploads/
!uploads/.gitkeep